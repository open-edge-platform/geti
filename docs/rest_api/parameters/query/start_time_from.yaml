in: query
name: start_time_from
style: form
description: Filter jobs which started after this time (inclusive). If time is not specified (only date), it defaults to the beginning of the day.
schema:
  $ref: "../../../../interactive_ai/services/api/schemas/parameters/timestamp.yaml"

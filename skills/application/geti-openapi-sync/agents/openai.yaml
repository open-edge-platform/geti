interface:
  display_name: "Geti OpenAPI Sync"
  short_description: "Sync backend OpenAPI output with UI types"
  default_prompt: "Use $geti-openapi-sync to regenerate and verify the backend and UI API contract."
